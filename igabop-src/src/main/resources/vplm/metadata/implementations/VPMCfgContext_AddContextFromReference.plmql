NAVIGATE
	T0.PLM_ExternalID,
	T0.PLMID,
	T1.PLM_ExternalID,
	T1.PLMID 
FROM
	VPMCfgContext/VPMCfgContext T0,
	PLMCORE/PLMCoreReference T1
FROM_ROOTS [?1?]
ON ( RELATION(
	FROM=PLMCORE/PLMCoreReference
	TO=VPMCfgContext/VPMCfgContext 
	THROUGH=POINTEDBY)
WITH QUALITY_FILTER (CATEGORY=ISOWNEDBY))
;

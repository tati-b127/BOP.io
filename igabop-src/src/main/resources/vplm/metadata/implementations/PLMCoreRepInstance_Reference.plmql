NAVIGATE
	T0.PLM_ExternalID,
	T0.PLMID 
FROM
	PLMCORE/PLMCoreRepReference T0
FROM_ROOTS [?1?]  
ON ( RELATION(
	FROM=PLMCORE/PLMCoreRepInstance
	TO=PLMCORE/PLMCoreRepReference
	THROUGH=POINTSTO)
WITH QUALITY_FILTER (CATEGORY=IsInstanceOf))
;

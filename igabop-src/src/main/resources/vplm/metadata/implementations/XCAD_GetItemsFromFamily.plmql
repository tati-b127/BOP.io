NAVIGATE 

T0.PLMID, 
T1.physicalid, 
T1.logicalid, 
T1.majorid, 
T1.type, 
T1.name,
T2.PLMID

FROM PLMCORE/PLMCoreRepReference T0, RelationClass/XCADItem T1, PLMCORE/PLMCoreReference T2
FROM_ROOTS [?1?]

ON ( RELATION ( FROM=PLMCORE/PLMCoreRepReference TO=RelationClass/XCADItem THROUGH=POINTEDBY ) WITH QUALITY_FILTER ( CATEGORY=ISOWNEDBY ) )
THEN ( RELATION ( FROM=RelationClass/XCADItem TO=PLMCORE/PLMCoreReference THROUGH=POINTSTO ) WITH QUALITY_FILTER ( CATEGORY=ISINSTANCEOF ) ) 

ITER=1;
